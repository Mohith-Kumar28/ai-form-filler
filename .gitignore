node_modules/
dist/
# WXT build output. `.output/` was the default and is kept here so pre-rename artifacts
# stay ignored; `apps/extension/build/` is the visible directory it moved to.
apps/extension/build/
.output/
.wxt/
.wrangler/
*.tsbuildinfo

# TanStack Start generated router
apps/web/src/routeTree.gen.ts
.react-start/

# Secrets — never commit. Worker secrets live in `wrangler secret put`.
# `.dev.vars.production` holds the LIVE Dodo key and signing secret.
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example

.DS_Store
coverage/
test-results/
playwright-report/

graphify-out/
.gallery/

# Intermediate captures for the store artwork. Rebuilt by `pnpm assets:store`; the finished
# PNGs beside them are committed, these are not.
store-assets/.raw/
